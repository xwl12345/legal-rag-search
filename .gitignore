# ── Build output ──
build/

# ── Compiled binaries ──
*.exe
*.dll
*.obj
*.o
*.a
*.lib
*.exp

# ── Qt generated files ──
moc_*
ui_*.h
qrc_*.cpp
*_autogen/

# ── IDE / Editor ──
.vscode/
.idea/
*.user
*.pro.user*
*.code-workspace

# ── OS files ──
.DS_Store
Thumbs.db
Desktop.ini

# ── Dependencies (third-party, already committed as submodules or copies) ──
# third_party/ 目录已随项目分发，不忽略

# ── Sensitive ──
# run.bat contains API key, be careful not to commit it publicly
run.bat

# ── User data ──
# Keep test fixtures versioned while excluding locally imported documents.
*.pdf
!test/data/*.pdf
docs/defense/pptx_render/

# ── ZCode 会话目录 ──
.zcode/

# ── AI 编码助手本地会话（不入库）──
.claude/

# ── 个人材料 / 答辩材料（仅本地，不入库 2026-09-17）──
.vs/
out/
.workbuddy/
docs/defense/
2027届毕业设计附件1-10：毕业论文（设计）过程管理表册/
毕业设计工作台/

# ── 运行期产物：索引落盘文件（T1，随程序运行生成，不入库）──
rag_index.dat
build/*.dat

# ── 运行期产物：问答历史 SQLite 库（T2，随程序运行生成，不入库）──
rag_history.db
test_history.db
build/*.db

# ── T2 导出的 Markdown（本地临时产物）──
docs/screenshots/*.md
